# ══════════════════════════════════════════════════════════
# 📚 My Bookshelves — Python Dependencies
# ══════════════════════════════════════════════════════════
# Install: python -m pip install --upgrade -r requirements.txt
# setup.bat/setup.sh also use --upgrade so an existing venv refreshes
# to the newest compatible package versions on each setup run.
# ══════════════════════════════════════════════════════════

# Cover extraction from PDF & EPUB
PyMuPDF>=1.23.0

# Image processing & WebP conversion
Pillow>=10.0.0

# Cover extraction from DOCX files
python-docx>=1.0.0
